'use strict';
const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('fs'); const path=require('path');
const root=path.resolve(__dirname,'..');
function sources(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>{const p=path.join(dir,e.name); if(['node_modules','.git','dist','test'].includes(e.name))return[]; return e.isDirectory()?sources(p):(e.name.endsWith('.js')?[p]:[]);});}
test('source tree contains no CNC mutation endpoints or FOCAS write calls',()=>{const text=sources(root).map(f=>fs.readFileSync(f,'utf8')).join('\n').toLowerCase(); const forbidden=[/\/activateprogram/,/\/deleteprogram/,/\/uploadprogram/,/\/writeprogram/,/\/setparameter/,/\/writeparameter/,/\/pmcwrite/,/\/setoffset/,/cnc_wrparam/,/cnc_download/,/pmc_wrpmcrng/]; forbidden.forEach(pattern=>assert.doesNotMatch(text,pattern));});
test('role matrix exposes no CNC mutation permission',()=>{const {MATRIX}=require('../lib/permissions'); assert.equal(Object.keys(MATRIX).some(k=>/cnc.*(write|control|mutate)|program.*(delete|activate|upload)/i.test(k)),false);});
test('remote display UI has no CNC keypad or command sender',()=>{const feature=fs.readFileSync(path.join(root,'src','js','features','cnc_live_remote.js'),'utf8'); const viewer=fs.readFileSync(path.join(root,'src','js','modules','vnc_viewer.js'),'utf8'); const actions=fs.readFileSync(path.join(root,'src','js','inline_actions.js'),'utf8'); assert.doesNotMatch(feature,/sendCncKeypress|Remote Keypad|tuş canlı CNC/i); assert.doesNotMatch(viewer,/sendCncKeypress|Tuş Komutu Gönderildi/i); assert.doesNotMatch(actions,/sendCncKeypress/); assert.match(viewer,/never sends a CNC command/);});
test('Drive sync merges business records without identity or secret data',()=>{const service=fs.readFileSync(path.join(root,'src','js','services','google_drive_sync.js'),'utf8'); const main=fs.readFileSync(path.join(root,'main.js'),'utf8'); assert.match(service,/mergeRecords/); assert.match(service,/revision/); assert.match(service,/updatedAt/); assert.match(service,/startAutoPolling/); assert.match(service,/driveSyncRequest/); assert.doesNotMatch(service,/State\?\.users|State\.users|apiKey|aiApiKey|pinHash|settings:\s*State/); assert.match(main,/script\.google\.com/); assert.match(main,/script\.googleusercontent\.com/);});
